name: Publish Public Index

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'catalog/**'
      - 'comparisons/**'
      - 'quality/module-maturity.yaml'
      - 'taxonomy/**'
      - 'scripts/build_comparisons.py'
      - 'scripts/build_depth_discovery.py'
      - 'scripts/build_index.py'
      - 'scripts/catalog_utils.py'
      - 'scripts/module_maturity.py'
      - 'scripts/taxonomy.py'
      - 'scripts/validate_catalog.py'
      - '.github/workflows/public-index.yml'

permissions:
  contents: write

concurrency:
  group: opendevindex-public-index
  cancel-in-progress: false

jobs:
  publish-index:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out main
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          ref: main

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: requirements-ci.txt

      - name: Install dependencies
        run: python -m pip install -r requirements-ci.txt

      - name: Generate public comparison views
        run: |
          python scripts/build_comparisons.py \
            --comparisons-dir comparisons \
            --catalog-dir catalog \
            --output-dir dist/comparisons \
            --public-dir docs/comparisons

      - name: Generate reviewed depth view
        run: |
          python scripts/build_depth_discovery.py \
            --catalog-dir catalog \
            --maturity-manifest quality/module-maturity.yaml \
            --output-dir dist/depth \
            --public-file docs/depth.md

      - name: Generate comparison-aware public module index
        run: |
          python scripts/build_index.py \
            --catalog-dir catalog \
            --output-dir dist/index \
            --public-index INDEX.md \
            --comparison-index dist/comparisons/module-comparisons.json

      - name: Publish only when discovery views changed
        run: |
          if [ -z "$(git status --porcelain -- INDEX.md docs/comparisons docs/depth.md)" ]; then
            echo 'Public index, comparison views, and reviewed depth view are already current.' >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          git config user.name "OpenDevIndex Publisher"
          git config user.email "207100624+BLCCoreStudio@users.noreply.github.com"
          git add INDEX.md docs/comparisons docs/depth.md
          git commit -m "docs: refresh public discovery views [skip ci]"
          git push origin HEAD:main
          echo 'Published updated INDEX.md, comparison views, and reviewed depth view.' >> "$GITHUB_STEP_SUMMARY"
