# OpenSSH

> Open-source implementation of the SSH protocol suite used for encrypted remote login, command execution, tunneling, and secure file transfer.

## Why it matters

OpenSSH is indexed by OpenDevIndex as a `security` knowledge module. This page is intentionally concise: it explains the technology's role, common uses, and high-signal facts while linking back to authoritative sources for details that can change over time.

## Typical use cases

- Connect securely to remote systems
- Forward ports through encrypted tunnels
- Transfer files with SSH-based tools

## Key points

- Public-key authentication is widely used
- The suite includes client and server components

## Primary links

- Homepage: https://www.openssh.com/

## Verified sources

- [OpenSSH official site](https://www.openssh.com/) — `official`
- [OpenSSH manual pages](https://man.openbsd.org/ssh) — `documentation`

## Maintenance

This module is independently versioned on branch `security/openssh`. When the technology, specification, project status, or canonical documentation changes, update this branch and refresh the verification date instead of silently changing unrelated modules.
