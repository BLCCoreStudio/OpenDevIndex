# Prettier

> Opinionated code formatter that parses source files and reprints them consistently, reducing style debates across JavaScript and many other supported languages.

## What it is

Prettier is indexed as a **tool**. Its stable OpenDevIndex address is `tool/prettier`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Apply consistent formatting across codebases
- Integrate automatic formatting into editors
- Enforce formatting in continuous-integration checks

## Key points

- Prettier intentionally limits formatting configuration
- It reformats parsed syntax rather than applying line-oriented rules
- A plugin system extends support to additional languages and file formats

## Taxonomy

- Kind: `tool`
- Domains: `developer-tools`, `web`
- Deployment: `cli`, `local`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://prettier.io/
- Repository: https://github.com/prettier/prettier

## Verified sources

- [Prettier official site](https://prettier.io/) — `official`
- [Prettier source repository](https://github.com/prettier/prettier) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
