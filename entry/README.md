# Milvus

> Open-source vector database built for large-scale similarity search over embeddings, with indexing, filtering, distributed deployment, and AI retrieval workflows.

## What it is

Milvus is indexed as a **database**. Its stable OpenDevIndex address is `database/milvus`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Store and search high-dimensional embeddings
- Build retrieval systems for generative AI applications
- Run large-scale approximate-nearest-neighbor search

## Key points

- Milvus supports multiple vector indexing strategies
- Metadata filtering can be combined with similarity search
- The system supports distributed deployments for larger workloads

## Taxonomy

- Kind: `database`
- Domains: `ai`, `data`
- Deployment: `self-hosted`, `service`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://milvus.io/
- Repository: https://github.com/milvus-io/milvus

## Verified sources

- [Milvus official site](https://milvus.io/) — `official`
- [Milvus source repository](https://github.com/milvus-io/milvus) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
