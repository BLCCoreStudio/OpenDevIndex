# Traefik Proxy

> Cloud-native reverse proxy and application proxy that discovers services dynamically and configures routing for containers and orchestration platforms.

## What it is

Traefik Proxy is indexed as a **tool**. Its stable OpenDevIndex address is `tool/traefik`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Route HTTP and TCP traffic to services
- Integrate ingress routing with container platforms
- Terminate TLS and apply middleware policies

## Key points

- Supports dynamic service discovery providers
- Frequently used with Kubernetes and container environments
- Configuration can combine static and dynamic sources

## Taxonomy

- Kind: `tool`
- Domains: `cloud`, `containers`, `devops`, `networking`
- Deployment: `self-hosted`
- License metadata: `MIT`

## Primary links

- Homepage: https://traefik.io/traefik
- Repository: https://github.com/traefik/traefik

## Verified sources

- [Traefik Proxy official site](https://traefik.io/traefik) — `official`
- [Traefik repository](https://github.com/traefik/traefik) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
