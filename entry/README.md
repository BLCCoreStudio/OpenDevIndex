# FFmpeg

> Cross-platform multimedia toolkit for decoding, encoding, transcoding, filtering, streaming, probing, and transforming audio and video formats.

## What it is

FFmpeg is indexed as a **tool**. Its stable OpenDevIndex address is `tool/ffmpeg`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Transcode audio and video files
- Build media-processing pipelines
- Inspect and stream multimedia content

## Key points

- Supports a very broad codec and container ecosystem
- Command-line tools are backed by reusable libraries
- Licensing of a particular build depends on enabled components

## Taxonomy

- Kind: `tool`
- Domains: `media`, `multimedia`
- Deployment: `cli`, `library`, `local`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://ffmpeg.org/
- Repository: https://github.com/FFmpeg/FFmpeg

## Verified sources

- [FFmpeg official site](https://ffmpeg.org/) — `official`
- [FFmpeg documentation](https://ffmpeg.org/documentation.html) — `documentation`
- [FFmpeg repository mirror](https://github.com/FFmpeg/FFmpeg) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
