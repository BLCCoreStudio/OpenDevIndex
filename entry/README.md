# Polars

> High-performance DataFrame library written in Rust with Python and other language bindings, built around columnar execution, query optimization, and Apache Arrow concepts.

## What it is

Polars is indexed as a **library**. Its stable OpenDevIndex address is `library/polars`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Transform large tabular datasets efficiently
- Build lazy query pipelines with optimizer support
- Use DataFrame workflows from Python or native Rust

## Key points

- Polars uses a columnar execution model
- Lazy queries allow optimization before execution
- The core engine is implemented in Rust

## Taxonomy

- Kind: `library`
- Domains: `analytics`, `data`
- Deployment: `library`, `local`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://pola.rs/
- Repository: https://github.com/pola-rs/polars

## Verified sources

- [Polars official site](https://pola.rs/) — `official`
- [Polars source repository](https://github.com/pola-rs/polars) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
