# SQLite

> Embedded relational database engine that stores a complete database in ordinary files and requires no separate database server process.

## What it is

SQLite is indexed as a **database**. Its stable OpenDevIndex address is `database/sqlite`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Embed relational storage inside applications
- Store local application state
- Use transactional SQL without running a server

## Key points

- Database files are portable across supported systems
- The library is linked directly into applications

## Taxonomy

- Kind: `database`
- Domains: `data`
- Deployment: not yet curated
- License metadata: `not yet curated`

## Primary links

- Homepage: https://www.sqlite.org/

## Verified sources

- [SQLite official site](https://www.sqlite.org/) — `official`
- [SQLite documentation](https://www.sqlite.org/docs.html) — `documentation`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
