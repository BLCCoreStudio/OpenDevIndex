# ESLint

> Pluggable static-analysis and linting tool for JavaScript and related ecosystems, used to detect problematic patterns and enforce project coding rules.

## What it is

ESLint is indexed as a **tool**. Its stable OpenDevIndex address is `tool/eslint`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Detect likely JavaScript code defects
- Enforce project-specific coding rules
- Integrate static checks with editors and CI pipelines

## Key points

- ESLint rules operate on parsed program syntax
- Flat configuration is the modern configuration model
- Plugins provide framework language and domain-specific rules

## Taxonomy

- Kind: `tool`
- Domains: `developer-tools`, `web`
- Deployment: `cli`, `local`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://eslint.org/
- Repository: https://github.com/eslint/eslint

## Verified sources

- [ESLint official site](https://eslint.org/) — `official`
- [ESLint source repository](https://github.com/eslint/eslint) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
