# WebAuthn

> W3C web authentication standard that enables public-key credentials and passkeys for phishing-resistant sign-in through browser and platform authenticator APIs.

## What it is

WebAuthn is indexed as a **standard**. Its stable OpenDevIndex address is `standard/webauthn`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Implement passkey-based user authentication
- Add hardware-backed public-key sign-in to web applications
- Reduce reliance on reusable passwords and phishing-prone credentials

## Key points

- WebAuthn uses asymmetric credentials scoped to relying parties
- Authenticators can be built into devices or external security keys
- The browser mediates credential creation and assertion ceremonies

## Taxonomy

- Kind: `standard`
- Domains: `security`, `web`
- Deployment: `web`, `service`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://www.w3.org/TR/webauthn-3/
- Repository: https://github.com/w3c/webauthn

## Verified sources

- [WebAuthn Level 3 specification](https://www.w3.org/TR/webauthn-3/) — `standard`
- [WebAuthn specification repository](https://github.com/w3c/webauthn) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
