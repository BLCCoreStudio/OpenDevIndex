# OpenSSL

> Open-source cryptography and TLS toolkit providing widely used libraries and command-line utilities for secure transport, certificates, and cryptographic operations.

## What it is

OpenSSL is indexed as a **library**. Its stable OpenDevIndex address is `security/openssl`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Implement TLS in applications
- Inspect and create certificates
- Perform cryptographic command-line operations

## Key points

- libcrypto provides cryptographic primitives
- libssl implements TLS-related protocol functionality

## Taxonomy

- Kind: `library`
- Domains: `security`
- Deployment: not yet curated
- License metadata: `not yet curated`

## Primary links

- Homepage: https://www.openssl.org/
- Repository: https://github.com/openssl/openssl

## Verified sources

- [OpenSSL official site](https://www.openssl.org/) — `official`
- [OpenSSL repository](https://github.com/openssl/openssl) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
