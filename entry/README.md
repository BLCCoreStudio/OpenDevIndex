# Deno

> JavaScript and TypeScript runtime with secure-by-default permissions, built-in developer tooling, web-standard APIs, and modern package compatibility.

## What it is

Deno is indexed as a **runtime**. Its stable OpenDevIndex address is `runtime/deno`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Run TypeScript and JavaScript services
- Build command-line applications
- Execute scripts with explicit filesystem and network permissions

## Key points

- Permissions restrict sensitive capabilities by default
- Includes formatter linter test runner and other tooling
- Supports many web-platform APIs

## Taxonomy

- Kind: `runtime`
- Domains: `programming-languages`, `web`
- Deployment: `local`, `self-hosted`
- License metadata: `MIT`

## Primary links

- Homepage: https://deno.com/
- Repository: https://github.com/denoland/deno

## Verified sources

- [Deno official site](https://deno.com/) — `official`
- [Deno repository](https://github.com/denoland/deno) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
