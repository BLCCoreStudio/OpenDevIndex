# XGBoost

> Optimized gradient-boosting library for decision-tree models with distributed, GPU, and language-binding support across common machine-learning workflows.

## What it is

XGBoost is indexed as a **library**. Its stable OpenDevIndex address is `library/xgboost`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Train gradient-boosted tree models
- Build ranking classification and regression systems
- Use CPU GPU and distributed training for tabular machine learning

## Key points

- XGBoost implements regularized gradient-boosted decision trees
- It supports sparse data and multiple execution backends
- Bindings are available for several common data-science languages

## Taxonomy

- Kind: `library`
- Domains: `ai`, `data`, `machine-learning`
- Deployment: `library`, `local`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://xgboost.ai/
- Repository: https://github.com/dmlc/xgboost

## Verified sources

- [XGBoost official site](https://xgboost.ai/) — `official`
- [XGBoost source repository](https://github.com/dmlc/xgboost) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
