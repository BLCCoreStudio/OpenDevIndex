# CMake

> Cross-platform build-system generator used to describe native software builds independently of a specific compiler, IDE, or underlying build tool.

## Why it matters

CMake is indexed by OpenDevIndex as a `tool` knowledge module. This page is intentionally concise: it explains the technology's role, common uses, and high-signal facts while linking back to authoritative sources for details that can change over time.

## Typical use cases

- Configure portable C and C++ builds
- Generate Ninja, Makefile, or IDE project files
- Discover dependencies during native builds

## Key points

- CMake generates build-system files rather than compiling directly
- CMakeLists.txt is the primary project description format

## Primary links

- Homepage: https://cmake.org/
- Repository: https://github.com/Kitware/CMake

## Verified sources

- [CMake official site](https://cmake.org/) — `official`
- [CMake documentation](https://cmake.org/cmake/help/latest/) — `documentation`

## Maintenance

This module is independently versioned on branch `tool/cmake`. When the technology, specification, project status, or canonical documentation changes, update this branch and refresh the verification date instead of silently changing unrelated modules.
