# spaCy

> Industrial natural-language-processing library for Python offering tokenization, linguistic pipelines, named-entity recognition, text classification, and extensible components.

## What it is

spaCy is indexed as a **library**. Its stable OpenDevIndex address is `library/spacy`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Build production NLP pipelines
- Perform tokenization tagging parsing and entity recognition
- Train and package custom text-processing components

## Key points

- spaCy organizes processing as configurable pipelines
- It ships language-specific models separately from the core library
- Custom components can combine trained and rule-based processing

## Taxonomy

- Kind: `library`
- Domains: `ai`, `machine-learning`, `python`
- Deployment: `library`, `local`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://spacy.io/
- Repository: https://github.com/explosion/spaCy

## Verified sources

- [spaCy official site](https://spacy.io/) — `official`
- [spaCy source repository](https://github.com/explosion/spaCy) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
