# uv

> Fast Python package and project manager from Astral that combines environment creation, dependency installation, lockfiles, tool execution, and Python version management.

## What it is

uv is indexed as a **tool**. Its stable OpenDevIndex address is `tool/uv`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Create and synchronize reproducible Python project environments
- Install and run Python command-line tools in isolated environments
- Manage Python versions, dependencies, lockfiles, and workspace-style projects from one CLI

## Key points

- uv is implemented in Rust and optimized for fast dependency and package operations
- It supports common Python packaging standards while combining workflows traditionally handled by several tools
- The project includes commands for projects, virtual environments, package installation, tools, and Python runtimes

## Taxonomy

- Kind: `tool`
- Domains: `cli`, `developer-tools`, `python`
- Deployment: `cli`, `local`
- License metadata: `Apache-2.0 OR MIT`

## Primary links

- Homepage: https://docs.astral.sh/uv/
- Repository: https://github.com/astral-sh/uv

## Verified sources

- [uv documentation](https://docs.astral.sh/uv/) — `documentation`
- [uv source repository](https://github.com/astral-sh/uv) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
