# NumPy

> Core Python library for multidimensional arrays, vectorized numerical operations, linear algebra, random sampling, and interoperability across scientific computing tools.

## What it is

NumPy is indexed as a **library**. Its stable OpenDevIndex address is `library/numpy`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Perform vectorized numerical computation
- Represent multidimensional scientific data as arrays
- Provide array interoperability for data-science and machine-learning libraries

## Key points

- NumPy ndarray is a foundational Python numerical data structure
- Vectorized operations move heavy computation into optimized native code
- A broad scientific Python ecosystem builds on NumPy array conventions

## Taxonomy

- Kind: `library`
- Domains: `data`, `python`
- Deployment: `library`, `local`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://numpy.org/
- Repository: https://github.com/numpy/numpy

## Verified sources

- [NumPy official site](https://numpy.org/) — `official`
- [NumPy source repository](https://github.com/numpy/numpy) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
