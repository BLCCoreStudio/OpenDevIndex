# LightGBM

> Gradient-boosting framework from Microsoft designed for efficient tree-based learning on large datasets, with support for classification, regression, and ranking.

## What it is

LightGBM is indexed as a **library**. Its stable OpenDevIndex address is `library/lightgbm`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Train boosted-tree models on large tabular datasets
- Build classification regression and ranking pipelines
- Use CPU or GPU acceleration where supported

## Key points

- LightGBM grows histogram-based decision trees
- It is designed for memory-efficient training on large datasets
- The project provides Python R and command-line interfaces

## Taxonomy

- Kind: `library`
- Domains: `ai`, `data`, `machine-learning`
- Deployment: `library`, `local`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://lightgbm.readthedocs.io/
- Repository: https://github.com/microsoft/LightGBM

## Verified sources

- [LightGBM documentation](https://lightgbm.readthedocs.io/) — `documentation`
- [LightGBM source repository](https://github.com/microsoft/LightGBM) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
