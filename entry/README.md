# SWC

> Rust-based platform for fast JavaScript and TypeScript compilation, minification, transformation, and extensible tooling used by modern web frameworks.

## What it is

SWC is indexed as a **tool**. Its stable OpenDevIndex address is `tool/swc`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Transpile modern JavaScript and TypeScript
- Minify application code
- Embed a fast compiler core into web frameworks and developer tools

## Key points

- SWC core compiler is implemented in Rust
- It supports syntax transforms and minification
- The project exposes APIs and plugins for integration into larger toolchains

## Taxonomy

- Kind: `tool`
- Domains: `build`, `developer-tools`, `web`
- Deployment: `cli`, `library`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://swc.rs/
- Repository: https://github.com/swc-project/swc

## Verified sources

- [SWC official site](https://swc.rs/) — `official`
- [SWC source repository](https://github.com/swc-project/swc) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
