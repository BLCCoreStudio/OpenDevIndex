# Reproducible Builds

> Software-build discipline in which independently repeated builds from the same source and inputs can produce bit-for-bit identical outputs.

## Why it matters

Reproducible Builds is indexed by OpenDevIndex as a `concept` knowledge module. This page is intentionally concise: it explains the technology's role, common uses, and high-signal facts while linking back to authoritative sources for details that can change over time.

## Typical use cases

- Verify distributed build artifacts
- Reduce hidden build-environment differences
- Strengthen software supply-chain auditing

## Key points

- Deterministic inputs and timestamps are important
- Independent rebuilds can help detect unexpected binary differences

## Primary links

- Homepage: https://reproducible-builds.org/

## Verified sources

- [Reproducible Builds project](https://reproducible-builds.org/) — `official`

## Maintenance

This module is independently versioned on branch `concept/reproducible-builds`. When the technology, specification, project status, or canonical documentation changes, update this branch and refresh the verification date instead of silently changing unrelated modules.
