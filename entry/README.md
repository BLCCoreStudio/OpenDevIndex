# InfluxDB

> Time-series database designed for metrics, events, observability, and sensor workloads, with specialized storage and query capabilities for timestamped data.

## What it is

InfluxDB is indexed as a **database**. Its stable OpenDevIndex address is `database/influxdb`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Store application and infrastructure metrics
- Collect sensor and IoT time-series data
- Build dashboards and alerting pipelines around timestamped measurements

## Key points

- InfluxDB is specialized for time-indexed data
- The project provides ingestion storage and query components
- It is frequently used in monitoring and telemetry stacks

## Taxonomy

- Kind: `database`
- Domains: `data`, `observability`
- Deployment: `self-hosted`, `service`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://www.influxdata.com/products/influxdb-overview/
- Repository: https://github.com/influxdata/influxdb

## Verified sources

- [InfluxDB official site](https://www.influxdata.com/products/influxdb-overview/) — `official`
- [InfluxDB source repository](https://github.com/influxdata/influxdb) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
