# Hatch

> Extensible Python project manager covering environments, packaging, versioning, scripts, builds, and publishing through a plugin-oriented workflow.

## What it is

Hatch is indexed as a **tool**. Its stable OpenDevIndex address is `tool/hatch`; taxonomy facets are kept separate from that address so classification can improve without breaking links.

## Why it matters

The module focuses on the technology's practical role, high-signal characteristics, common use cases, and authoritative references. Fast-changing details should be verified against the sources below rather than inferred from stale copies.

## Typical use cases

- Manage Python development environments
- Build and publish Python packages
- Define repeatable project scripts and versioning workflows

## Key points

- Hatch uses pyproject.toml as its primary project configuration
- Its environment system supports multiple Python configurations
- Plugins extend build versioning metadata and environment behavior

## Taxonomy

- Kind: `tool`
- Domains: `developer-tools`, `python`
- Deployment: `cli`, `local`
- License metadata: `not yet curated`

## Primary links

- Homepage: https://hatch.pypa.io/
- Repository: https://github.com/pypa/hatch

## Verified sources

- [Hatch documentation](https://hatch.pypa.io/) — `documentation`
- [Hatch source repository](https://github.com/pypa/hatch) — `repository`

## Verification

The catalog metadata and source references for this module were reviewed on **2026-08-31**. Automated checks validate structure and source reachability; factual updates still require source-backed editorial review.

## Maintenance

Update this independently versioned module when material facts, project status, canonical documentation, or important trade-offs change. Preserve the stable module address unless a compatibility migration is explicitly documented.
