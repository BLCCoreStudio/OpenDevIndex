# History

## 2026-08-31 — Initial schema v3 module

- Added Apache Parquet under the canonical `format/` namespace.
- Mapped the module to `databases-data` coverage topics `data-formats` and `analytics`.
- Verified the module against Apache Parquet project documentation and the canonical format repository.
