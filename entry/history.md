# History

## 2026-08-31 — Initial schema v3 module

- Added JSON under the canonical `format/` namespace.
- Mapped the module to `standards-protocols-formats` coverage topics `data-formats` and `serialization`.
- Verified the module against IETF RFC 8259 and ECMA-404.
