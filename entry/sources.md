# Sources

Verified on **2026-08-31**.

- **Apache Parquet overview** — https://parquet.apache.org/docs/overview/ (`official`)
- **Apache Parquet file format documentation** — https://parquet.apache.org/docs/file-format/ (`documentation`)
- **Apache Parquet format repository** — https://github.com/apache/parquet-format (`repository`)

The project documentation describes Parquet as a column-oriented format and points to the `parquet-format` repository as the specification source. Feature support should be checked against the implementations used in a real pipeline.
