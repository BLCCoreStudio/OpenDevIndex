# Sources

Deep-dive verification completed on **2026-09-01**. Sources below are maintained by the Git project or point to the canonical source repository.

## Core documentation

- **Git official site** — https://git-scm.com/ (`official`)
- **Pro Git book** — https://git-scm.com/book/en/v2 (`documentation`)
- **Git command documentation** — https://git-scm.com/docs (`documentation`)
- **Canonical Git source repository** — https://github.com/git/git (`repository`)

## Internals

- **Plumbing and Porcelain** — https://git-scm.com/book/en/v2/Git-Internals-Plumbing-and-Porcelain (`documentation`)
- **Git Objects** — https://git-scm.com/book/en/v2/Git-Internals-Git-Objects (`documentation`)
- **Git References** — https://git-scm.com/book/en/v2/Git-Internals-Git-References (`documentation`)
- **Packfiles** — https://git-scm.com/book/en/v2/Git-Internals-Packfiles (`documentation`)
- **Index format** — https://git-scm.com/docs/index-format (`documentation`)
- **Pack format** — https://git-scm.com/docs/gitformat-pack (`documentation`)

## Networking and repository formats

- **Git transfer protocols** — https://git-scm.com/book/en/v2/Git-on-the-Server-The-Protocols (`documentation`)
- **Hash-function transition** — https://git-scm.com/docs/hash-function-transition (`documentation`)
- **git-fsck** — https://git-scm.com/docs/git-fsck (`documentation`)
- **git-config** — https://git-scm.com/docs/git-config (`documentation`)

## Editorial use

These sources support the module's explanations of the object model, index, references, commit graph, packfiles, protocols, repository integrity, recovery, and repository-format evolution.

Version-sensitive claims should be rechecked against current upstream documentation before changing this module. Secondary tutorials are intentionally not used as the primary authority for Git internals.
